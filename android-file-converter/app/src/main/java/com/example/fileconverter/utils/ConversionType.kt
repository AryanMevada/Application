package com.example.fileconverter.utils

/**
 * Supported conversion options shown in Spinner.
 */
enum class ConversionType(val label: String) {
    IMAGE_TO_PDF("Image to PDF"),
    PDF_TO_IMAGE("PDF to Image"),
    TXT_TO_PDF("TXT to PDF")
}
